export enum MessageDefault {
    COMPANY_TO_PAY = 'Empresa a la que realizaras el pago',
}

export enum MessageError {
    COMPANY_EMPTY = 'Debes completar este campo',
    COMPANY_NOT_FOUND = 'No encontramos ninguna empresa con este nombre',
}

export enum StateError {
    ERROR = 'error',
    NO_ERROR = '',
}